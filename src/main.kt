fun main(){
    val s = ""
    val v = "  "
    println(s.isEmpty())
    println(s.isBlank())
    println(s.isNullOrBlank() || s.isNullOrEmpty())
    println(v.isEmpty())
    println(v.isBlank())
    println(s.length)
    println(v.length)
}